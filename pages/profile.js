import Header from "@l/header";
import Navigator from "@l/navigator";

export default function Profile() {

    return (
        <div>
            <Header />
            Profile
            <Navigator />
        </div>
    );
}